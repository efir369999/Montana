language = "C"
include_guard = "MONTANA_FFI_H"
autogen_warning = "/* Сгенерировано cbindgen из mt-bindings (network.rs/mdns.rs). Не редактировать вручную — SSOT в Rust-коде. Регенерация: cbindgen --config cbindgen.toml crates/mt-bindings -o crates/mt-bindings/include/montana_ffi.h */"
pragma_once = false
tab_width = 4
style = "type"

[export]
prefix = ""
